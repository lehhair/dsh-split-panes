{
  "note": "The deepseek-harness release this plugin is built and released against. Keep in sync with src/client/vendor/renderer (gated by `pnpm run vendor:renderer:check`); rewritten by scripts/upgrade-core.mjs. It lives here rather than inside a workflow file because the default GITHUB_TOKEN cannot push changes under .github/workflows — a tracker PR that edited the pinned ref in build-release.yml was rejected by GitHub.",
  "tag": "dsh-v0.1.5-rc.1",
  "ref": "183f08e9c6dde7e36cd2318eaee70b0da08fb35e"
}
